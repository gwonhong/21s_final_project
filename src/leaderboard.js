let scores=[];
function loadScores(){
  let lastScores = localStorage.getItem("scores");
  scores=JSON.parse(lastScores);
  scores.forEach();
}
function saveScores(){

}

function scoreToBoard(){
  let score ={
    time: time,
    score: score
  };
}